{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make re && make clean",
            // "args": ["re", "clean"],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "envFile": "${workspaceFolder}/.env",
            "detail": "Compiles the minishell project using Makefile"
        }
    ]
}
